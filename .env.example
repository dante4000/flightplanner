# Copy to .env and fill in. .env is gitignored — never commit real credentials.
# Get your key from https://seats.aero → Settings → API Key (Pro plan).
SEATS_AERO_KEY=pro_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
